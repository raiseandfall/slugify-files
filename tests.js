'use strict';

var assert = require('assert'),
  fs = require('fs'),
  sanitize = require('./');

describe('api', function() {
  it('should rename file', function() {
    assert.equal(1 === 1, true);
  });
});

